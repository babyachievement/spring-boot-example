create TABLE book(id  BIGINT generated by default as identity (start with 1),
reader varchar(64),
isbn VARCHAR (64),
title VARCHAR (64),
author VARCHAR (64),
description VARCHAR(128),
  primary key (id)
);